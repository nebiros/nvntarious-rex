<h2><%= t("editing_product") %></h2>
<div class="btn-group" role="group" aria-label="">
  <%= link_to t("show"), @product, {class: "btn btn-secondary"} %>
  <%= link_to t("back"), products_path, {class: "btn btn-secondary"} %>
</div>

<hr/>

<%= render 'form', product: @product %>
